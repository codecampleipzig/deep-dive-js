# Questions

1. What are the complexities of these array operations

```
array[i];
// O(n)

array.push(x)
// O(1)

array.shift(x)
// O(n)

array.map(x => 2 * x)
// O(n)

array.includes(x)
// O(n)

array.sort()
// O(n log n) (take a guess)
```
